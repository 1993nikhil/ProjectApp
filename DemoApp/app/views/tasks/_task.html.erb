<%= form_for [@project, Task.new], remote: true do |f| %>
  <%= f.check_box :complete %>
  <%= task.name %>
  <%= link_to "(remove)", [@project, Task.new], method: :delete, data: {confirm: "Are you sure?"}, remote: true %>
<% end %>
