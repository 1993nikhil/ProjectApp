<%= form_for [@project, task], remote: true do |f| %>
  <%= f.check_box :complete, :onclick => "this.form.submit();" %>
  <%= task.name %>
  <%= link_to "(remove)", [@project, task], method: :delete, data: {confirm: "Are you sure?"}, remote: true %>
<% end %>


